{
  "name": "@webcore/backend",
  "version": "0.0.1",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "dev": "echo 'Backend dev TBD (e.g., vercel dev)'",
    "build": "tsc -p .",
    "lint": "eslint . --ext .ts --fix",
    "format": "prettier --write \"**/*.{ts,json,md}\"",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@vercel/node": "^5.1.14"
  },
  "dependencies": {
    "@webcore/shared": "workspace:^"
  }
}